# ─── Post-Quantum Cryptography ───────────────────────────────────────────────
# liboqs-python wraps the Open Quantum Safe C library.
# It provides ML-KEM-768, ML-DSA-65, and SPHINCS+ (SLH-DSA proxy).
# Install guide at bottom of this file.
liboqs-python==0.15.0

# ─── Symmetric crypto + KDF ───────────────────────────────────────────────────
pycryptodome==3.20.0        # AES-256-GCM bulk encryption
cryptography==42.0.5        # HKDF with SHA3-256, secure random, PBKDF2

# ─── Serialisation ────────────────────────────────────────────────────────────
msgpack==1.0.8              # compact binary framing for messages

# ─── Server ───────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ─── Client ───────────────────────────────────────────────────────────────────
httpx==0.27.0               # async-capable HTTP client

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ══════════════════════════════════════════════════════════════════════════════
# HOW TO INSTALL ON YOUR MACHINE
# ══════════════════════════════════════════════════════════════════════════════
#
# 1. Install system dependencies (Ubuntu/Debian):
#
#    sudo apt-get update
#    sudo apt-get install -y cmake ninja-build libssl-dev python3-dev
#
#    macOS (Homebrew):
#    brew install cmake ninja openssl
#
#    Windows:
#    Install CMake from https://cmake.org/download/
#    Install Visual Studio Build Tools (C++ workload)
#    Install OpenSSL: https://slproweb.com/products/Win32OpenSSL.html
#
# 2. Create and activate a virtual environment:
#
#    python3 -m venv venv
#    source venv/bin/activate        # Linux / macOS
#    venv\Scripts\activate           # Windows
#
# 3. Install Python dependencies (this auto-builds liboqs C library):
#
#    pip install -r requirements.txt
#
# 4. Verify the PQ library loaded correctly:
#
#    python3 -c "
#    import oqs
#    k = oqs.KeyEncapsulation('ML-KEM-768')
#    s = oqs.Signature('ML-DSA-65')
#    print('All PQ algorithms available. Ready to build.')
#    "
#
# ══════════════════════════════════════════════════════════════════════════════